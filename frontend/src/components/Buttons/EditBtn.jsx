// ------------------------------- Edit Button Component, to allow updating of item
const EditBtn = ({editItem}) => (
    <button onClick={editItem} className="editBtn">Edit</button>
);

export default EditBtn;